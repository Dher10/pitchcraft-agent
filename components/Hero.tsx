export default function Hero() {
  return (
    <section>
      <p className="mb-1 text-xs font-medium uppercase tracking-widest text-neutral-500">
        CACHED DEMO
      </p>
      <h1 className="mb-1 text-3xl font-bold text-neutral-900">
        PitchCraft Matchup Agent
      </h1>
      <p className="text-sm text-neutral-600">
        Cached matchup data becomes a readable report, then a critic checks the claims against the same data.
      </p>
    </section>
  );
}
